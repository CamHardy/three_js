// main.js

// do nothing (for now).
window.onload = function () {
	var text = document.createTextNode('This script is doing nothing (for now).');
	document.getElementById('the_zone').appendChild(text);
}